# Laundry Machine Timer - Environment Configuration
# Copy this file to .env and modify the values as needed

# =============================================================================
# Server Configuration
# =============================================================================

# Port for the HTTP server to listen on
PORT=3000

# Host address to bind the server to (0.0.0.0 for all interfaces)
HOST=0.0.0.0

# Node.js environment (development, production, test)
NODE_ENV=development

# CORS origin (use * for all origins, or specify specific domains)
CORS_ORIGIN=*

# =============================================================================
# Database Configuration
# =============================================================================

# Path to the SQLite database file
DATABASE_PATH=./data/laundry.db

# Database connection timeout in milliseconds
DATABASE_TIMEOUT_MS=30000

# =============================================================================
# Timer Configuration
# =============================================================================

# How often to check for expired timers (in milliseconds)
TIMER_CHECK_INTERVAL_MS=30000

# Maximum timer duration in minutes (default: 5 hours)
MAX_TIMER_DURATION_MINUTES=300

# =============================================================================
# Logging Configuration
# =============================================================================

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# =============================================================================
# Application Configuration
# =============================================================================

# Graceful shutdown timeout in milliseconds
SHUTDOWN_TIMEOUT_MS=10000

# =============================================================================
# Production Configuration Examples
# =============================================================================

# For production deployment, you might use:
# NODE_ENV=production
# PORT=8080
# HOST=0.0.0.0
# LOG_LEVEL=warn
# DATABASE_PATH=/var/lib/laundry-timer/laundry.db
# CORS_ORIGIN=https://yourdomain.com

# For Turso (cloud SQLite) deployment:
# DATABASE_PATH=libsql://your-database-url
# DATABASE_AUTH_TOKEN=your-auth-token